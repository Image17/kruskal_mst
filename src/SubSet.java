
public class SubSet {
	int parent, rank;
	
	/**
	 * Public constructor to initialize a SubSet with 
	 * given parent and rank
	 * @param parent
	 * @param rank
	 */
	public SubSet(int parent, int rank) {
		this.parent = parent;
		this.rank = rank;
	}
}
